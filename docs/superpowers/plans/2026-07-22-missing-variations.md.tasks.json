{
  "planPath": "docs/superpowers/plans/2026-07-22-missing-variations.md",
  "tasks": [
    {
      "id": 21,
      "subject": "V1: port bubble (CPU + Metal) — flips 05739/31943 to .gate",
      "status": "completed",
      "description": "**Goal:** Port flam3 `bubble` (var28, variations.c:671-678; paramless, 0 RNG draws) to CPU `Variations.swift` table + Metal `Kernels.metal`. Flip 05739 + 31943 from `.knownGap` to `.gate` (they use only bubble). This alone makes the motion-blur clip genomes faithful.\n\n**Formula:** `r = weight / (0.25 * (p.x*p.x+p.y*p.y) + 1); return SIMD2(r*p.x, r*p.y)`.\n\n**Files:** VariationDescriptor.swift (canonicalOrder + `d(\"bubble\",[],[:])`), Variations.swift (table), Kernels.metal (NUM_XFORM_SLOTS_MS 33→34, `v_bubble` + dispatch line), VariationsTests.swift (closed-form hand-traced), VariationDescriptorTests.swift + ParamChannelParityTests.swift (bump 33→34), SpecialSauceParityTests.swift (testBubble ≥38dB), RealGenomeParityTests.swift (flip 05739/31943 → .gate; update 00000 reason to remove bubble).\n\n**Acceptance Criteria:** CPU closed-form test passes (hand-traced from var28_bubble); Metal↔CPU parity testBubble ≥38 dB; 05739+31943 flipped to .gate both ≥38 dB/0.95 SSIM; make test-fast green; goldens unchanged (additive).\n\n**Verify:** swift test --filter VariationsTests --filter VariationDescriptorTests --filter ParamChannelParityTests --filter SpecialSauceParityTests --filter RealGenomeParityTests (sandbox OFF).\n\n```json:metadata\n{\"files\": [\"Sources/FlameKit/VariationDescriptor.swift\", \"Sources/FlameKit/Variations.swift\", \"Sources/FlameRenderer/Metal/Kernels.metal\", \"Tests/FlameKitTests/VariationsTests.swift\", \"Tests/FlameRendererTests/RealGenomeParityTests.swift\"], \"verifyCommand\": \"swift test --filter VariationsTests --filter SpecialSauceParityTests --filter RealGenomeParityTests\", \"acceptanceCriteria\": [\"bubble CPU closed-form\", \"bubble Metal↔CPU ≥38dB\", \"05739+31943 flipped to .gate ≥38dB\", \"slot count 33→34\", \"make test-fast green\", \"goldens unchanged\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 22,
      "subject": "V2: port eyefish (CPU + Metal; NOT a fisheye alias)",
      "status": "completed",
      "blockedBy": [21],
      "description": "**Goal:** Port flam3 `eyefish` (var27, variations.c:659-669; paramless, 0 RNG draws). NOT an alias of fisheye — output is UN-SWAPPED `(r*p.x, r*p.y)` vs fisheye's swapped `(r*p.y, r*p.x)`. Unblocks 00000 (partially).\n\n**Formula:** `r = (weight*2)/(sqrt(p.x*p.x+p.y*p.y) + 1); return SIMD2(r*p.x, r*p.y)` (no axis swap).\n\n**Files:** VariationDescriptor.swift (canonicalOrder + descriptor; count→35), Variations.swift (table), Kernels.metal (count→35, v_eyefish + dispatch), VariationsTests.swift (closed-form + ASSERT eyefish≠fisheye for non-axis-symmetric input), VariationDescriptorTests.swift + ParamChannelParityTests.swift (bump→35), SpecialSauceParityTests.swift (testEyefish ≥38dB), RealGenomeParityTests.swift (correct the \"(alias of fisheye)\" comment at :77).\n\n**Acceptance Criteria:** CPU closed-form passes; eyefish≠fisheye assertion passes (un-swapped); Metal↔CPU ≥38dB; count→35; make test-fast green; goldens unchanged.\n\n```json:metadata\n{\"files\": [\"Sources/FlameKit/Variations.swift\", \"Sources/FlameRenderer/Metal/Kernels.metal\", \"Tests/FlameKitTests/VariationsTests.swift\"], \"verifyCommand\": \"swift test --filter VariationsTests --filter SpecialSauceParityTests\", \"acceptanceCriteria\": [\"eyefish CPU closed-form\", \"eyefish≠fisheye (un-swapped)\", \"eyefish Metal↔CPU ≥38dB\", \"slot count→35\", \"make test-fast green\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 23,
      "subject": "V3: port pie (3 params, 3 ordered RNG draws)",
      "status": "completed",
      "blockedBy": [22],
      "description": "**Goal:** Port flam3 `pie` (var37, variations.c:795-809; params pie_slices=6/pie_rotation=0/pie_thickness=0.5; 3 isaac_01 draws in EXACT order). RNG-consuming → goes in the `evaluate` switch, NOT the table. Unblocks 00000 (partially).\n\n**3 draws in order:** (1) `sl=(Int)(isaac_01*slices+0.5)`; (2) `isaac_01*thickness` angular offset; (3) `weight*isaac_01` radial. Then `a = rotation + 2π*(Double(sl)+d2*thickness)/slices; r = weight*d3; return (r*cos(a), r*sin(a))`.\n\n**Files:** VariationDescriptor.swift (canonicalOrder + descriptor; count→36), Variations.swift (evaluate switch + static `pie(_:weight:params:rng:)` mirroring julian; 3 rng.isaac01() in order), Kernels.metal (count→36, v_pie(p,w,pr,rng) with 3 isaac_01(rng) in order + dispatch `if (w[35]!=0) acc += v_pie(pre,w[35],&x.varParams[35*8],rng)`), VariationsTests.swift (closed-form fixed-stream + draw-count-3 test per testJulianDrawsOneAndFinite), VariationDescriptorTests.swift + ParamChannelParityTests.swift (bump→36), SpecialSauceParityTests.swift (testPie ≥38dB).\n\n**Acceptance Criteria:** CPU draws exactly 3 isaac_01 in flam3 order (draw-count test); closed-form fixed-stream passes; Metal↔CPU ≥38dB; in evaluate switch (not table); count→36; make test-fast green.\n\n```json:metadata\n{\"files\": [\"Sources/FlameKit/Variations.swift\", \"Sources/FlameRenderer/Metal/Kernels.metal\", \"Tests/FlameKitTests/VariationsTests.swift\"], \"verifyCommand\": \"swift test --filter VariationsTests --filter SpecialSauceParityTests\", \"acceptanceCriteria\": [\"pie 3 ordered draws\", \"pie CPU closed-form\", \"pie Metal↔CPU ≥38dB\", \"in evaluate switch\", \"slot count→36\", \"make test-fast green\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 24,
      "subject": "V4: port radial_blur (1 param, 4 summed RNG draws)",
      "status": "pending",
      "blockedBy": [23],
      "description": "**Goal:** Port flam3 `radial_blur` (var36, variations.c:775-793; param radial_blur_angle=0; 4 isaac_01 draws summed left-to-right). RNG-consuming → evaluate switch. Unblocks 00000 (partially).\n\n**4 draws summed:** `rndG = weight*(d1+d2+d3+d4-2.0)`. `spinvar=sin(angle*π/2); zoomvar=cos(angle*π/2); ra=sqrt(p.x²+p.y²); tmpa=atan2(p.y,p.x)+spinvar*rndG; rz=zoomvar*rndG-1; return (ra*cos(tmpa)+rz*p.x, ra*sin(tmpa)+rz*p.y)`.\n\n**Files:** VariationDescriptor.swift (canonicalOrder + descriptor; count→37), Variations.swift (evaluate switch + static `radialBlur(_:weight:params:rng:)`; 4 rng.isaac01() summed in order), Kernels.metal (count→37, v_radial_blur(p,w,pr,rng) with 4 isaac_01(rng) summed + dispatch), VariationsTests.swift (closed-form + draw-count-4 test), VariationDescriptorTests.swift + ParamChannelParityTests.swift (bump→37), SpecialSauceParityTests.swift (testRadialBlur ≥38dB).\n\n**Acceptance Criteria:** CPU draws exactly 4 isaac_01 summed as weight*(d+d+d+d-2) (draw-count test); closed-form passes; Metal↔CPU ≥38dB; count→37; make test-fast green.\n\n```json:metadata\n{\"files\": [\"Sources/FlameKit/Variations.swift\", \"Sources/FlameRenderer/Metal/Kernels.metal\", \"Tests/FlameKitTests/VariationsTests.swift\"], \"verifyCommand\": \"swift test --filter VariationsTests --filter SpecialSauceParityTests\", \"acceptanceCriteria\": [\"radial_blur 4 summed draws\", \"radial_blur CPU closed-form\", \"radial_blur Metal↔CPU ≥38dB\", \"slot count→37\", \"make test-fast green\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 25,
      "subject": "V5: flip 00000 to .gate + final verify + re-render faithful clip",
      "status": "pending",
      "blockedBy": [21, 22, 23, 24],
      "description": "**Goal:** With all 4 variations in, flip 00000 to `.gate` (all 7 fixtures ≥38 dB); run full parity verification; re-render the now-faithful Task-4 clip (05739→31943, bubble faithful) for the user's visual confirmation.\n\n**Files:** RealGenomeParityTests.swift (flip 00000 → .gate).\n\n**Acceptance Criteria:** all 7 RealGenomeParityTests fixtures ≥38 dB/0.95 SSIM; GoldenParity unchanged (51-72 dB); make test-fast green; AnimationParityTests (M3 vs flam3) unchanged; re-rendered clip boundary PSNR still smooth (≤ within-loop + ~1 dB); user visual confirms clip faithful to official 05739/31943 videos.\n\n**Verify:** swift test --filter RealGenomeParityTests (all 7 ≥38) + swift test --filter GoldenParityTests + make test-fast; then re-render clip (release) + boundary PSNR + open for user.\n\n```json:metadata\n{\"files\": [\"Tests/FlameReferenceTests/RealGenomeParityTests.swift\"], \"verifyCommand\": \"swift test --filter RealGenomeParityTests && swift test --filter GoldenParityTests\", \"acceptanceCriteria\": [\"all 7 fixtures ≥38dB\", \"GoldenParity unchanged\", \"make test-fast green\", \"clip boundary smooth\", \"user visual faithful\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-07-22T00:00:00Z"
}
