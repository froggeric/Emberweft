import Foundation

/// EmberweftUI — SwiftUI/AppKit GUI support library for Emberweft.
///
/// Hosts the `FlameUI`↔SwiftUI bridge, the production playback conformers
/// (`MetalFrameRenderer`, `CPUFrameRenderer`, `WallClock`), the thumbnail
/// service, and the library/settings models. Reused by the GUI app (M4) and
/// the screensaver (M5). This file is a module marker; real types live in
/// their dedicated files.
public enum EmberweftUI {}
